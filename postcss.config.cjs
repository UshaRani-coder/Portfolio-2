module.exports = {
  plugins: [
    require('tailwindcss'), // Include Tailwind CSS plugin
    require('autoprefixer'), // Include Autoprefixer plugin
    // Add other PostCSS plugins if needed
  ],
}
